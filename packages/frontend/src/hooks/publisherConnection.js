import { useState, useEffect, ussRef } from "react"
import { useToasts } from "react-toast-notifications"
import socket from "./socket"

function usePublisherConnection({ streamId }) {
    
    }, [])

    return response
}

export default usePublisherConnection
